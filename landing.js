// If you want more advanced effects later, we can add here
console.log("Landing page loaded!");

function toggleMenu() {
  const navLinks = document.getElementById("navLinks");
  navLinks.classList.toggle("show");
}

